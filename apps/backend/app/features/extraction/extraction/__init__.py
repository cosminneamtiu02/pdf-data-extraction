"""LangExtract orchestration wrapper (PDFX-E004-F003).

This engine package and
`app/features/extraction/intelligence/ollama_gemma_provider.py` are the
only files in the extraction feature permitted to import `langextract`.
"""
